name: CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  scan_ruby:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Scan for common Rails security vulnerabilities using static analysis
        run: bin/brakeman --no-pager
      
      - name: Scan for known security vulnerabilities in gems used
        run: bin/bundler-audit
      
  scan_js:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Check JavaScript syntax
        run: find app/javascript test -type f \( -name '*.js' -o -name '*.mjs' \) -print0 | xargs -0 -n1 node --check

      - name: Run JavaScript unit tests
        run: find test/unit -maxdepth 1 -type f -name '*.mjs' -print0 | sort -z | xargs -0 -n1 node

      - name: Scan for security vulnerabilities in JavaScript dependencies
        run: bin/importmap audit

  browser_loop_record:
    runs-on: ubuntu-latest
    timeout-minutes: 8

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare app
        run: bin/rails db:prepare

      - name: Start Rails
        run: |
          bin/rails server -b 127.0.0.1 -p 3000 > /tmp/cassio-rails.log 2>&1 &
          for i in {1..40}; do
            if curl --fail --silent http://127.0.0.1:3000/up >/dev/null; then exit 0; fi
            sleep 0.5
          done
          cat /tmp/cassio-rails.log
          exit 1

      - name: Exercise REC count-in with live pad and piano input
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/loop_record_interactive.mjs

      - name: Print Rails log on failure
        if: failure()
        run: cat /tmp/cassio-rails.log || true

  browser_responsive_layout:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare app
        run: bin/rails db:prepare

      - name: Start Rails
        run: |
          bin/rails server -b 127.0.0.1 -p 3000 > /tmp/cassio-rails.log 2>&1 &
          for i in {1..40}; do
            if curl --fail --silent http://127.0.0.1:3000/up >/dev/null; then exit 0; fi
            sleep 0.5
          done
          cat /tmp/cassio-rails.log
          exit 1

      - name: Check portrait chassis, keyboard, and menu containment
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/responsive_layout.mjs

      - name: Verify searchable full-screen manual
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/manual.mjs

      - name: Verify LOOP sequencer discoverability
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/loop_sequencer_discovery.mjs

      - name: Verify PROJECT V1 save/open/import/export lifecycle
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/project_v1.mjs

      - name: Verify unnamed PROJECT sessions cannot be replaced silently
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/project_unsaved_guard.mjs

      - name: Verify SETTINGS V1 controls, chassis color, and reload persistence
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/settings_v1.mjs

      - name: Verify chassis rendering and PLAY metronome SETTINGS sync
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/settings_bridge.mjs

      - name: Verify pointer, touch, mapped-key, HOLD, and TAP/BPM feedback
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/input_feedback.mjs

      - name: Verify post-PR12 startup, track clear/visibility, dim, and dev gating
        env:
          CASSIO_URL: http://127.0.0.1:3000/
          CHROME_BIN: google-chrome
        run: node test/smoke/post_pr12_stabilization.mjs

      - name: Print Rails log on failure
        if: failure()
        run: cat /tmp/cassio-rails.log || true

  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Lint code for consistent style
        run: bin/rubocop -f github
