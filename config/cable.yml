# Async is sufficient for the current single-web-dyno prototype. Move to a
# shared adapter before scaling CASSIO across multiple web dynos.
development:
  adapter: async

test:
  adapter: test

production:
  adapter: async
